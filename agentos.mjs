#!/usr/bin/env node

/**
 * AgentOS ESM CLI entrypoint.
 *
 * Keep a single command registration and bootstrap path. The canonical
 * implementation lives in main.js; this file exists only for compatibility
 * with the `agentos-esm` binary.
 */
import './main.js';
