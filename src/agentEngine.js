/**
 * @deprecated Compatibility entry point.
 *
 * The canonical AgentEngine lives in src/core/agentEngine.js.
 * Keep this path only for consumers that have not migrated yet.
 */
export { AgentEngine, TurnResult, UsageSummary } from './core/agentEngine.js';
