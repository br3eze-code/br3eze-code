# Payment Phase A

PaymentPlatform is canonical. PaymentGateway is compatibility-only. Provider implementations and idempotency belong to the canonical platform. Remaining runtime caller migration is Phase B.